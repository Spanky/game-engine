#include "StableHeaders.h"
#include "GO_PropagationRow.h"

namespace GO
{
	//PropagationRow::PropagationRow(size_t aColumnCount)
	//	: myCells(aColumnCount)
	//{
	//	size_t capacityBefore = myCells.capacity();

	//	myCells.shrink_to_fit();

	//	size_t capacityAfter = myCells.capacity();
	//	GO_ASSERT(capacityBefore == capacityAfter, "Initial capacity of myCells was too large");
	//}
}